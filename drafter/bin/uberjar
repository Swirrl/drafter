#!/usr/bin/env bash

set -o errexit

mkdir -p target/classes

clojure -A:aot
clojure -C:aot:prod -A:pack:uber --application-version="$(git describe --always)" target/drafter.jar

echo Uberjarring complete
